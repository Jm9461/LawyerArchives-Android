<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/editCaseTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="عنوان پرونده"
        android:inputType="textCapSentences"/>

    <EditText
        android:id="@+id/editSessionDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="تاریخ جلسه (مثلاً 1403/05/10)"
        android:inputType="date"/>

    <EditText
        android:id="@+id/editLocation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="محل جلسه"
        android:inputType="textPostalAddress"/>

    <EditText
        android:id="@+id/editDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="شرح جلسه"
        android:inputType="textMultiLine"
        android:minLines="3"
        android:gravity="top"/>

    <CheckBox
        android:id="@+id/checkboxCompleted"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="تکمیل شده"
        android:layout_marginTop="8dp"/>

    <Button
        android:id="@+id/saveSessionButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ذخیره تغییرات"
        android:layout_marginTop="16dp"/>
</LinearLayout>